<%= stylesheet_link_tag 'public/tags' %>
<div class="container">
  <div class="row">
    <h1>タグ一覧</h1>
    <div>
      <% @tags.each do |tag| %>
        <%= link_to tag.word, tag_path(tag), class: 'tag' %>
      <% end %>
    </div>
  </div>
</div>